package com.example.supergame;

public class Wendigo extends Enemy {
    public Wendigo(String name, int maxHp, int currentHp, int damage, int accuracy) {
        super(name, maxHp, currentHp, damage, accuracy);
    }
}
