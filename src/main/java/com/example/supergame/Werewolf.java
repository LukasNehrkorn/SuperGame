package com.example.supergame;

public class Werewolf extends Enemy {
    public Werewolf(String name, int maxHp, int currentHp, int damage, int accuracy) {
        super(name, maxHp, currentHp, damage, accuracy);
    }
}
